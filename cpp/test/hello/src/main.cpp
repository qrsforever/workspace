#include <string.h>
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    char buff[1024] = { 0 };    
    char* t1 = (char*)malloc(10);
    while (1) sleep(1);
    return 0;
}
